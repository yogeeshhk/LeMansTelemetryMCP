# ngrok v3 forwards requests to the loopback-only MCP server with a valid Host.
on_http_request:
  - actions:
      - type: add-headers
        config:
          headers:
            host: "127.0.0.1:18765"
